namespace DangerBug.Core.Models;

// Base class for every bug. The traits shared by all insects live here.
// TODO (par-prog): add the shared properties — name, legs, can-bite, can-fly,
// can-move, plague, good-stuff, risk level — plus a virtual Describe().
public class Bug
{
}
